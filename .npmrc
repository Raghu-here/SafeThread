# Force npm to use a flat structure which helps Vercel's prefix logic
install-strategy=hoisted
# Prevent the idealTree conflict by disabling the tracker in CI
progress=false
# Avoid redundant lockfile updates that crash Vercel
package-lock=false
